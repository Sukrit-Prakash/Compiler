#  regular expressions use a small set of special characters to represent a wide range of text patterns
#  r"\d" to prevent bashlashes from being misinterpreted 
#  r"a-zA-Z_] matches a single lowercase letter, uppervase or underscore
#  r"." matches a single character
#  r"\d+" matches one or more digits
